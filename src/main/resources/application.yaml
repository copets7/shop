spring:
  jpa:
    hibernate:
     ddl-auto: validate
    show-sql: true
  datasource:
     url: jdbc:mysql://localhost:3306/shop
     username: root
     password: 70286_CopetS
  flyway:
    baseline-on-migrate: true
